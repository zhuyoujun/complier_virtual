#include<stdio.h>
#include"show_bytes.c"

/*if the machine is shift right logical,return 1,otherwise return 0.
*/
int int_shifts_are_logical()
{
  int x = ~0;
  return (x>>1)>0;
}

main()
{ 
  int result = int_shifts_are_logical();
  printf("result = %d\n",result);
}
